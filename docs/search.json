{"config":{"lang":["en"],"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"python-project-sandbox","text":"<p>A modern Python project base for experimenting with tooling — powered by uv, Ruff, mypy and pytest, with test coverage and HTML reports published to dedicated branches via GitHub Actions.</p>","path":["Home"],"tags":[]},{"location":"#highlights","level":2,"title":"Highlights","text":"<ul> <li>Fast tooling — <code>uv</code> for dependency management, <code>Ruff</code> for lint/format,   <code>mypy</code> for type checking, <code>just</code> as the task runner.</li> <li>Branch-managed reports — coverage summaries are published to the   <code>coverage</code>   branch and pytest HTML/cov reports to the   <code>ghpages</code>   branch. See Reports &amp; coverage for live links and the   pytest-html styling setup, and CI &amp; Coverage for the pipeline.</li> <li>Modern tooling, adopted early — <code>zizmor</code> (Actions security), <code>ty</code>   (Astral type checker), <code>pip-audit</code>, <code>typos</code> and <code>validate-pyproject</code> run as a   preview quality workflow.</li> <li>Multi-OS / multi-Python CI — every change is tested across   Ubuntu / macOS / Windows on Python 3.11, 3.12 and 3.13.</li> </ul>","path":["Home"],"tags":[]},{"location":"#where-to-next","level":2,"title":"Where to next","text":"<ul> <li>Getting started — install dependencies and run the   common development tasks.</li> <li>CI &amp; Coverage — how coverage and reports are built and published.</li> </ul> <p>Documentation engine</p> <p>This site is built with Zensical, the next-generation static site generator from the creators of Material for MkDocs, adopted here as an early-access (preview) tool.</p>","path":["Home"],"tags":[]},{"location":"ci/","level":1,"title":"CI &amp; Coverage","text":"<p>This project leans on GitHub Actions for testing and for publishing reports to dedicated branches — a core idea of this template.</p>","path":["CI & Coverage"],"tags":[]},{"location":"ci/#workflows","level":2,"title":"Workflows","text":"Workflow Trigger Purpose <code>PR Check (Except Dependabot)</code> PR to <code>main</code> Multi-OS / multi-Python tests, 90% coverage gate, coverage PR comment <code>Dependabot PR Check</code> Dependabot PR Same checks for dependency-update PRs <code>Test Multi-OS</code> push to <code>main</code> Tests + publishes the coverage summary to the <code>coverage</code> branch <code>Modern Quality (Preview)</code> PR / push <code>zizmor</code> / <code>ty</code> / <code>pip-audit</code> / <code>typos</code> / <code>validate-pyproject</code> (preview) <code>semantic-release</code> manual (<code>workflow_dispatch</code>) Conventional-Commits release: version bump, CHANGELOG, tag + GitHub Release <code>Docs (Zensical)</code> push / PR Build this site and deploy to <code>ghpages/docs/</code> <p>All test workflows install <code>just</code> from a prebuilt binary (<code>taiki-e/install-action</code>) and use <code>concurrency</code> to cancel superseded runs, keeping CI fast.</p>","path":["CI & Coverage"],"tags":[]},{"location":"ci/#branch-managed-reports","level":2,"title":"Branch-managed reports","text":"<p>Rather than committing generated artifacts to <code>main</code>, reports live on their own branches:</p> <ul> <li><code>coverage</code> — a generated <code>README.md</code> with the per-OS / per-Python coverage   summary. See the   coverage branch.</li> <li><code>ghpages</code> — pytest HTML and coverage reports published to GitHub   Pages, driven by pytest-testmon   incremental runs. See the   reports.</li> </ul> <p>This documentation site is published to the <code>docs/</code> subdirectory of the <code>ghpages</code> branch, so it coexists with the pytest reports served from the root.</p>","path":["CI & Coverage"],"tags":[]},{"location":"ci/#releases","level":2,"title":"Releases","text":"<p>Releases are manually triggered with semantic-release using Conventional Commits (adapted from json2vars-setter). Run the <code>semantic-release</code> workflow from the Actions tab (<code>workflow_dispatch</code>); it then determines the next version from the commits since the last tag, bumps <code>pyproject.toml</code> (via <code>uv version</code>) and <code>uv.lock</code>, updates <code>CHANGELOG.md</code>, commits them back to <code>main</code> (<code>[skip ci]</code>), and creates the <code>vX.Y.Z</code> tag and GitHub Release. Nothing is released until you trigger the workflow.</p>","path":["CI & Coverage"],"tags":[]},{"location":"ci/#coverage-pr-comments","level":2,"title":"Coverage PR comments","text":"<p>Pull requests receive a coverage comment from <code>pytest-coverage-comment</code>. File links are resolved with <code>coverage-path-prefix: project_a/</code> so they point at real source paths, and a <code>cleanup_pr_comments</code> job removes stale comments on re-runs to avoid broken links.</p>","path":["CI & Coverage"],"tags":[]},{"location":"getting-started/","level":1,"title":"Getting started","text":"","path":["Getting started"],"tags":[]},{"location":"getting-started/#install","level":2,"title":"Install","text":"<p>This project uses uv for fast Python package management.</p> ProductionDevelopment <pre><code># Install uv if not already installed\ncurl -LsSf https://astral.sh/uv/install.sh | sh\n\n# Install runtime dependencies\nuv sync\n</code></pre> <pre><code># Install dependencies including development tools\nuv sync --extra dev\n</code></pre>","path":["Getting started"],"tags":[]},{"location":"getting-started/#task-runner-just","level":2,"title":"Task runner (just)","text":"<p>Common tasks are wrapped with just:</p> <pre><code>just --list            # List all available commands\njust testcov           # Run tests with coverage\njust lint              # Lint with Ruff\njust format            # Format with Ruff\njust type-check        # Type check with mypy\njust check             # Lint + type-check + tests\n</code></pre>","path":["Getting started"],"tags":[]},{"location":"getting-started/#modern-tooling-preview","level":3,"title":"Modern tooling (preview)","text":"<p>Early-adopted quality tools are also available as <code>just</code> recipes:</p> <pre><code>just type-check-ty     # Type check with ty (Astral, preview)\njust spell             # Spell check with typos\njust audit-actions     # GitHub Actions security audit (zizmor)\njust audit-deps        # Dependency vulnerability scan (pip-audit)\njust validate-pyproject # Validate pyproject.toml schema\n</code></pre>","path":["Getting started"],"tags":[]},{"location":"getting-started/#pre-commit","level":2,"title":"Pre-commit","text":"<pre><code># Install dependencies and the pre-commit hooks\nuv sync --extra dev\nuv run pre-commit install\n\n# Run all hooks against every file\nuv run pre-commit run --all-files\n</code></pre>","path":["Getting started"],"tags":[]},{"location":"getting-started/#build-these-docs-locally","level":2,"title":"Build these docs locally","text":"<p>The documentation site is built with Zensical:</p> <pre><code>uvx zensical serve     # Live preview at http://127.0.0.1:8000\nuvx zensical build     # Build static site into ./site\n</code></pre>","path":["Getting started"],"tags":[]},{"location":"reports/","level":1,"title":"Reports &amp; coverage","text":"<p>This project keeps report generation independent (each report is produced by a different tool across an OS / Python matrix) but uses this documentation site as the hub that links them together. Nothing is coupled at build time — the docs simply point at the published artifacts.</p>","path":["Reports & coverage"],"tags":[]},{"location":"reports/#what-is-published-and-where","level":2,"title":"What is published, and where","text":"Report Tool Location Coverage summary <code>pytest-coverage-comment</code> <code>coverage</code> branch README Coverage HTML <code>pytest-cov</code> <code>ghpages</code> → <code>pytest-cov-report/&lt;os&gt;/python/&lt;version&gt;/</code> Test report HTML <code>pytest-html</code> <code>ghpages</code> → <code>pytest-html-report/&lt;os&gt;/python/&lt;version&gt;/</code> <p>The HTML reports are driven by pytest-testmon incremental runs and deployed per OS / Python version. The canonical index — kept up to date with the current matrix — lives in the <code>ghpages</code> branch README.</p>","path":["Reports & coverage"],"tags":[]},{"location":"reports/#live-links","level":3,"title":"Live links","text":"<ul> <li>Indexes (recommended entry points):<ul> <li>Coverage summary → https://github.com/7rikazhexde/python-project-sandbox/tree/coverage?tab=readme-ov-file#pytest-coverages-summary</li> <li>HTML / cov reports → https://github.com/7rikazhexde/python-project-sandbox/tree/ghpages?tab=readme-ov-file#pytest-report</li> </ul> </li> <li>Direct URL pattern (replace <code>&lt;os&gt;</code> / <code>&lt;version&gt;</code>):<ul> <li>Test report: <code>…/python-project-sandbox/pytest-html-report/&lt;os&gt;/python/&lt;version&gt;/report_page.html</code></li> <li>Coverage:    <code>…/python-project-sandbox/pytest-cov-report/&lt;os&gt;/python/&lt;version&gt;/index.html</code></li> </ul> </li> <li>Concrete example (ubuntu-latest / Python 3.11.9):<ul> <li>Test report</li> <li>Coverage</li> </ul> </li> </ul> <p>Why not serve reports from this site?</p> <p>The reports are large, self-contained HTML generated independently for each of <code>ubuntu</code> / <code>macos</code> / <code>windows</code> × Python <code>3.12</code> / <code>3.13</code> (and more), using <code>pytest-testmon</code> incremental state. Keeping generation separate avoids coupling two pipelines and bloating the docs build — this page is the hub, the reports stay independent.</p>","path":["Reports & coverage"],"tags":[]},{"location":"reports/#pytest-html-styling-custom-css","level":2,"title":"pytest-html styling (custom CSS)","text":"<p>The HTML test report is themed with a custom stylesheet at <code>css/style.css</code>. It is applied with pytest-html's <code>--css</code> option, and <code>--self-contained-html</code> inlines the CSS so the published <code>report_page.html</code> is a single portable file:</p> <pre><code>uv run pytest \\\n  --html=htmlcov/report_page.html \\\n  --self-contained-html \\\n  --css=css/style.css \\\n  --capture=no \\\n  project_a tests/\n</code></pre> <p>Or via the task runner:</p> <pre><code>just testhtml          # -&gt; htmlcov/report_page.html (styled)\n</code></pre> <p>In CI, the same <code>--css=css/style.css</code> is passed in <code>test_pytest-html-report_deploy_multi_os.yml</code> before the report is published to <code>ghpages</code>.</p> <p>Reproduce this setup in your own project</p> <ol> <li>Add a stylesheet (e.g. <code>css/style.css</code>).</li> <li>Generate the report with <code>--html=… --self-contained-html --css=css/style.css</code>.</li> <li>Publish the per-matrix output to a <code>ghpages</code> subdirectory (this project    uses <code>pytest-&lt;html|cov&gt;-report/&lt;os&gt;/python/&lt;version&gt;/</code>), keeping existing    files so the reports and these docs coexist.</li> <li>Link everything from a single hub page — like this one.</li> </ol>","path":["Reports & coverage"],"tags":[]}]}